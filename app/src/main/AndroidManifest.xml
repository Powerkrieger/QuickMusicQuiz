<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Spotify Web API calls and token exchange over HTTPS -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickMusicQuiz">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QuickMusicQuiz"
            android:launchMode="singleTop">
            <!--
                launchMode="singleTop": when the Spotify browser redirects back to our app
                via the custom URI scheme and the activity is already on top of the stack,
                Android calls onNewIntent() instead of creating a new activity instance.
            -->

            <!-- Standard launcher intent — opens the app from the home screen -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Handles the redirect from Spotify's login browser page back to our app.

                After the user approves the Spotify login, the browser navigates to:
                    quickmusicquiz://callback?code=AUTHORIZATION_CODE

                Android matches the URI scheme + host to this intent-filter and delivers
                the intent to MainActivity (via onCreate or onNewIntent).
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="quickmusicquiz"
                    android:host="callback" />
            </intent-filter>
        </activity>
    </application>

</manifest>
